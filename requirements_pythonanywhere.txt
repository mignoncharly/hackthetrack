# Racing AI - PythonAnywhere Requirements
# Optimized for free tier (512MB RAM limit)
#
# Install with: pip install -r requirements_pythonanywhere.txt

# Web Framework
flask>=2.3.0
flask-cors>=4.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# Optional: SHAP for model explanations (may be memory-intensive)
# shap>=0.44.0

# Note: The following packages are NOT needed for PythonAnywhere:
# - flask-socketio (WebSockets not supported on free tier)
# - eventlet (WebSocket async support)
# - uvicorn (ASGI server for FastAPI)
# - fastapi (we embed the API in Flask)
# - python-socketio (WebSocket client)
# - streamlit (separate app framework)
